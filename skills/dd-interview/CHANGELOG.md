# dd_interview 版本更新记录

> 完整版本历史记录。优化Skill或回溯变更时读取此文件。

## 版本记录

**V6.0（2026-09-15）— 开源重构：过程管控型 → 验收契约型**

- 共享规范引用（S1/S3-S7/S9/S10/S12）整体替换为 `../risk-workflow/CONTRACT.md` 单一契约
- 零信息损耗铁律保留（语义原样）；验收改为引用 **CONTRACT §6 AC1 三件套**（关键实体全量比对、兜底板块存在且非空、数字精确性），六条红线内容并入"验收语义展开"
- 七步法精简为五步法：删除"步骤1 不直接输出正文（防截断）"与"步骤7 清空上下文"
- 纠错字典外置为用户可维护文件 `{workspace}/transcribe_glossary.json`，SKILL 内仅保留 3 条通用示例条目（机构简称/产品代号/人名地名的上下文纠错思路），删除项目专属条目
- risk_points.json 取消，语义（四维度归纳 + 汇总 + 待核实覆盖）并入 `interview_result.json`（含 pending_items_results 结构化回写）；残留未核实项落 `pending_items.json`（CONTRACT §1 衔接）；output.json 改 CONTRACT §3 最小 schema
- 四维度强制标题/空维度注明、大纲确认日志等阻断性要求降为建议性表述；检查点 CP2.3/CP2.4 签名模板删除（CONTRACT §4.2 子确认 + CP2 主检查点）；启动前置检查删除（CONTRACT §9）
- Word 生成与宿主路径解耦（CONTRACT §8），md 先行，docx 变可选输出；转换命令块删除
- 脱敏：示例中真实公司名（2 处）、人名（17 处）及纠错字典真实条目（约 10 条）替换为占位/通用表述

**V5.1（2026-05-04）— 控制论复盘优化·8项机械化约束升级**

背景：workflow-retro 控制论校验发现 output.json/risk_points.json 在历次执行中从未产出，模块B归纳格式不统一。

- **O1** ✅：output.json 列为检查点A/B阻断项（缺失不可通过）
- **O2** ✅：risk_points.json 列为检查点B阻断项，新增完整 schema（dimensions→4维度数组+summary+pending_items_coverage）
- **O3** ✅：模块B四维度标题升级为强制结构（不可省略，无发现也必须保留标题注明"未涉及"）
- **O4** ✅：关注要点汇总表（高度/一般关注）+待核实覆盖表升级为阻断项，增加判定标准
- **O5** ✅：interview_result 从自由文本→结构化 schema（response/evidence/risk_assessment/confidence/source_session）
- **O6** ✅：步骤2大纲确认增加结构化确认日志模板（写入 project_log.md，含板块数/兜底板块/待核实覆盖/知识库加载状态/确认人/时间）
- **O7** ✅：启动前置检查新增 Dossier存在性断言 + skill_version校验 + Phase目录检查 + 场景判断日志
- **O8** ✅：批量中断恢复增加备选路径（output.json不存在时基于文件系统检查）
- 检查点A/B增加签名模板（CP-A/CP-B），签名缺失=检查点未通过
- 版本号 V5.0→V5.1

**V4.1（2026-04-26）— K2.6 review后 Agent 宿主落地优化（P3）**
- P3：模块B dossier更新后新增质量检查项，包含"冲突检测已执行"（遵循S5冲突检测规则，访谈新发现与历史信息矛盾已标注）

**V4.2（2026-04-26）— 版本号更新**
- 版本号同步更新（无功能变更）

**V4.3（2026-04-26）— dossier更新增强**
- 模块A每场纪要新增dossier更新（§4/§7-§10/§15）
- 模块B新增dossier更新（§12-§14/§0）

**V4.0（2026-04-12）— 从dd_interview拆分：模块A迁出至dd_check，新增待核实事项融入**
- 模块A（资料核查）整体迁出至独立Skill dd_check V1.0
- 新增输入：pending_items.json（dd_check输出），作为访谈重点关注方向
- 新增逻辑：待核实事项融入（大纲覆盖+重写标注+校验+归纳覆盖情况）
- 模块重新编号：原模块B→模块A，原模块C→模块B
- 启动对话流程更新：移除底稿资料/补充清单收集项，新增pending_items路径
- 执行模式更新：标准模式从"模块A→B→C"改为"模块A→B"
- 删除：资料核查逻辑（已迁出）、公司底稿读取场景

**V3.5（2026-04-11）— P2优化项修复**
- P2-8：新增"转写稿来源适配"段落，明确通义听悟为主要来源（带说话人标签+段落分隔+语气词已处理，但专业术语错别字多）
- P2-8：新增通义听悟常见纠错规则：行业/公司/人名纠错、技术术语纠错、数字金额纠错，纠错在重写过程中自然完成
- 纠错字典保留并增强，结合dd_prep的im_report和qcc_cache中公司全称进行校正

**V3.4（2026-04-11）— P0优化项修复**
- P0-1：补充尽调模式从"重新执行标准模式，独立输出"改为"增量模式"（追加补充尽调专节，输出supplement_session.md + risk_points_delta.json）
- 启动对话流程同步更新补充尽调表述

**V3.3 (2026-04-08) — PaddleOCR 强制调用 + QCC 缓存路径补全**
- 新增 PDF/图像文件读取规则：禁止大模型直接读取扫描 PDF，强制调用 PaddleOCR-VL API
- 补充模块A资料核查中的企查查缓存路径声明
- 适用于：访谈转写稿（扫描PDF）、公司底稿（盖章扫描件）

**V3.2 (2026-04-08) — 批量处理 + 多场次归纳重构**
- 新增启动对话流程（材料收集步骤）
- 新增批量处理机制：支持文件夹输入，逐份执行，避免超限
- 模块C重构：从"单份纪要风险提取"改为"多场次关注要点归纳"
- 归纳维度扩展：法律合规 / 投资风险 / 上市障碍 / 可持续经营（四维度）
- 弱化与risk_report的格式绑定：本模块只归纳关注要点，不做风险定性
- 补充尽调模式改为独立流程：重新执行标准模式，不在已有纪要上增量
- 明确"不额外校对转写稿"：纠错在重写过程中自然完成
- 保留17大类风险分类映射表作为参考（见附录），但不再作为模块C的强制分类

**V3.1 (2026-04-08) — 执行模式扩展**
- 新增三种执行模式：标准/补充尽调/加急
- 补充尽调模式：仅做增量核查，更新风控要点
- 加急模式：仅关键风险领域

**V3.0 (2026-04-07) — 零信息损耗重构**
- 删除了独立的 `interview-minutes-from-transcript` 冗余 Skill。
- 确立了 V3.0 流程：语义分析 -> 动态大纲 -> 无损重写 (禁止摘要、禁止虚词) -> 单文件 Word 直出。
- 修复了长文档分割拼接导致文件损坏的技术问题。
- 整合了 17 大风险分类映射表。

## 附录：17大类风险分类映射表（参考用）

| 大类 | 识别关键词 | 纪要提取示例 |
|------|------|------|
| **主体与股权** | 无实控人、控制权不稳定、一致行动 | "创始人间有股权调整机制" |
| **上市合规** | 未盈利、18C、对赌临界 | "准备 2026 年申报但不满 2 年" |
| **资产独立性** | 关联交易、客户集中>50% | "客户 A 占比 77%" |
| **财务与税务** | 收入确认、资金归集、递延收益 | "资金池被母公司调用未付利息" |

> 此表仅供模块C归纳时参考，不作为强制分类体系。实际归纳应按访谈内容组织。

**V5.0（2026-04-26）— DRY重构**
- 引入 _shared_specs.md 共享规范引用（S1/S3/S4/S5/S6/S7/S10/S12），替代内联共享规则
- 精简结构：上下游关系→表格、执行模式→表格、纠错规则→分类表格
- 文档结构说明恢复封面要素表+标题层级表
- 补充尽调模式恢复范围限定
- 所有功能细节完整保留，零丢失
- 515行→约418行（压缩19%），零功能损失
