# SECURITY_PRIVACY.md — 数据分级与外传边界

本仓库默认一切尽调材料敏感，默认全部本地处理。任何把材料内容送出本机的操作都需要显式确认。本文是 [CONTRACT.md](skills/risk-workflow/CONTRACT.md) §7（适配器层）数据边界的总说明。

## 1. 数据分级

| 级别 | 内容 | 外部服务处理 |
|------|------|--------------|
| **S1 默认禁传** | 合同/协议等交易文件、员工花名册、财务报表、资产与 IP 清单、访谈纪要与转写稿、外部尽调报告（律所/会所）、投资建议书/BP、任何含真实个人信息或未公开项目事实的材料 | 默认禁止上传任何外部服务（含外部 OCR、第二信源），仅本地 OCR 或人工转录，无法处理时按 AC5 降级标注。**信任路径**：使用者已核验服务商承担保密义务且承诺不用于模型训练（如企业版协议、数据处理协议、私有化部署）的，在 config 声明 `paddleocr.s1_policy: trusted-provider` 后，S1 材料经每次 `--allow-external-upload` 显式确认可上传 |
| **S2 确认后可传** | 已确认公开可获得的材料：上市公司公告、监管与政府公示文件、招股说明书、工商公示档案等 | 经显式确认后可上传外部 OCR（`paddleocr_api.py --allow-external-upload`）；来源公开但文件含未公开项目信息的，按 S1 处理 |
| **S3 元数据级** | URL 列表、检索关键词、知识库名称 | 经显式确认后可发送第二信源（`ima_kb_archive.py --allow-external-send`） |
| **S4 公开** | 法规原文检索、公开行业信息 | 检索类适配器正常使用 |

分级按文件内容性质判断（是否含未公开项目事实或个人信息），不依据文件类型是否"看起来公开"：尽调报告、投资建议书、访谈材料即使来自第三方也属 S1。脚本层无法自动判断内容性质，因此入口处一律要求人工显式确认；技能层的分类边界见 CONTRACT §7.2。

## 2. 默认值与开关

| 配置 | 默认 | 说明 |
|------|------|------|
| `ima.ssl_verify` | `true` | TLS 证书校验默认开启；设为 `false` 会打印醒目警告，仅限受控代理环境 |
| `paddleocr.allow_upload` | `false` | 外部 OCR 全局免确认开关，慎用 |
| `paddleocr.s1_policy` | `deny` | `deny`=S1 敏感材料一律禁传；`trusted-provider`=使用者已核验服务商保密义务与"不用于训练"承诺后放行（上传时仍须逐次 `--allow-external-upload` 确认） |
| `ima.allow_send` | `false` | 第二信源导入全局免确认开关，慎用 |

原则：不安全模式与外传模式默认关闭，开启必须显式（CLI flag 或 config 布尔值），并在运行时输出警告。

## 3. 凭据与会话文件

- API 凭据经环境变量或仓库外文件读取（`~/.config/ima/` 等），`config.yaml` 已被 .gitignore 排除，凭证不入库。
- 浏览器会话目录（cookies/localStorage）自动设置 0700/0600 权限并原子写入；路径经 `ima.session_dir` 配置。
- 凭据仅随对应 API 请求发送到对应服务，不写入日志、产出文件或缓存。

## 4. 保留与日志

- 项目工作区产物（Dossier、evidence_registry、报告底稿）保存在项目目录，不回传仓库；仓库只含模板与通用方法。
- qcc 缓存（qcc_cache*.json）为公开工商信息，已列入 .gitignore。
- 脚本输出不打印凭据值；错误信息中的 URL 与响应片段只保留排障所需最小内容。

## 5. OCR 与外部服务的替换

- 外部 OCR 适配器（PaddleOCR API）是可插拔实现；S1 材料在任何实现下都不外传。
- 当前版本没有内置本地 OCR 引擎；S1 扫描件的本地 OCR 依赖宿主环境自带能力，缺失时按 AC5 降级（显式标注"未核查+原因"），禁止为完成核查而外传。
- 集成任何新外部服务前，先对照本文件分级表确定级别并补默认关闭的确认门槛。
